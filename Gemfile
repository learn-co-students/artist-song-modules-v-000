source "https://rubygems.org"

# gem "rails"
#guard used to show me the passing tests, but now it only shows the failing

gem 'rspec'
gem 'pry'

 gem 'rb-fsevent', :require => false if RUBY_PLATFORM =~ /darwin/i
 gem 'guard-rspec'
 gem 'guard-livereload'